# The raw Kaggle archive is ~9.5 GB of JSON -- never commit it. Only the 20
# gzipped sample replays under data/sample/ belong in the repo.
data/raw/
data/processed/
data/sample_processed/
/*.json
**/archive/
*.json.gz
!data/sample/*.json.gz

# Per-row test predictions are regenerated by `make train` (a few MB).
reports/test_predictions.csv.gz

__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/
.DS_Store
