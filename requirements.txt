# Core pipeline. Versions are floors, not pins -- the code uses stable APIs.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
matplotlib>=3.8

# Optional: enables Parquet output via `--parquet`. The pipeline writes
# gzipped CSV when this is absent, so it is not required.
# pyarrow>=15.0
